{
  "name": "cdc-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm --parallel -r build",
    "lint": "pnpm --parallel -r lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\" --ignore-path .gitignore",
    "db:migrate": "pnpm --filter @cdc/api prisma:migrate",
    "db:seed": "pnpm --filter @cdc/api db:seed",
    "db:reset-admin": "pnpm --filter @cdc/api db:reset-admin",
    "db:bootstrap": "pnpm --filter @cdc/api db:bootstrap",
    "db:studio": "pnpm --filter @cdc/api prisma studio"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
